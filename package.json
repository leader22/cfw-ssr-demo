{
  "private": true,
  "scripts": {
    "dev": "nuxi dev",
    "dev:worker": "miniflare .output/server/index.mjs --site .output/public",
    "build": "NITRO_PRESET=cloudflare nuxi build",
    "start": "node .output/server/index.mjs"
  },
  "devDependencies": {
    "miniflare": "^2.2.0",
    "nuxt3": "latest"
  }
}
